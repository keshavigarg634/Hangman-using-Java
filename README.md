git config --global user.name "Keshavi Garg"
git config --global use# Hangman Game (Java)

A console-based Hangman game developed in Java.

## Features
- Interactive gameplay with word guessing.
- Includes sound effects (`win.wav` and `lose.wav`).
- Built with modular logic (`GameLogic`, `WordGenerator`).

## How to Run
1. Open the project in your IDE (like VS Code or IntelliJ).
2. Compile the files.
3. Run the `HangmanGame.java` file.r.email "keshavigarg634@GMAIL.COM"
